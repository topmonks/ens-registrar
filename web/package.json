{
  "name": "web2",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "eth-ens-namehash": "^2.0.8",
    "font-awesome": "^4.7.0",
    "ncp": "^2.0.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "^2.1.5",
    "web3": "^1.0.0-beta.43"
  },
  "scripts": {
    "copy": "mkdir .\\src\\contracts && ncp ../build/contracts src/contracts",
    "start": "rm -rf src/contracts && npm run copy && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
